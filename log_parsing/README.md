# Log Parsing

## Description
This project contains a Python script (`0-stats.py`) that reads logs from `stdin`, processes them line by line, and computes relevant metrics. The script continuously tracks the total file size and counts of various HTTP status codes. The statistics are printed every 10 lines and upon receiving a keyboard interruption (`CTRL + C`).

# UTF-8 Validation

## Overview
This project implements a function to determine whether a given dataset represents a valid UTF-8 encoding. The dataset consists of integers where each integer represents a byte (8-bit value). 

## Prototype
```python
def validUTF8(data)
